require "spec_helper"

describe Env::Checker do
  it "has a version number" do
    expect(Env::Checker::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
